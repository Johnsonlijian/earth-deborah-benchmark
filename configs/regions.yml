regions:
  global:
    description: "Global first-pass domain where supported by the source catalog."
    bounds: null

  example_plate_boundary:
    description: "Optional plate-boundary sensitivity template; provide verified bounds before enabling."
    bounds:
      minlatitude: null
      maxlatitude: null
      minlongitude: null
      maxlongitude: null

  example_river_group:
    description: "Optional river-group template; provide verified station identifiers before enabling."
    station_ids: []
